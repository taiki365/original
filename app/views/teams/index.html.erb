<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="Mark Otto, Jacob Thornton, and Bootstrap contributors">
    <meta name="generator" content="Hugo 0.79.0">
    <title>Dashboard Template · Bootstrap v5.0</title>

    <link rel="canonical" href="https://getbootstrap.com/docs/5.0/examples/dashboard/">

    

  </head>
  <body>
    
<header class="navbar navbar-dark sticky-top bg-dark flex-md-nowrap p-0 shadow">
  
</header>


  

    <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
      <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">チーム成績</h1>
      </div>

      <div class="table-responsive">
        <table class="table table-striped table-sm">
          <thead>
            <tr> 
              <th>チーム名</th>
              <th>試合数</th>
              <th>勝利</th>
              <th>敗北</th>
              <th>引き分け</th>
              <th>勝率</th>
              <th>勝率差</th>
              <th>チーム防御率</th>
              <th>チーム打率</th>
              <th>チーム本塁打</th>
              <th>チーム盗塁</th>
              <th>チーム得点</th>
              <th>チーム失点</th>
            </tr>
          </thead>
          <% @team.each do |t| %>
          <tbody>
       
            <tr>
              <td><%= t.team_name.name %></td>
              <td><%= t.game_number %></td>
              <td><%= t.team_win %></td>
              <td><%= t.team_lose %></td>
              <td><%= t.draw %></td>
              <td><%= t.win_rate %></td>
              <%# <td><%= t. </td> %>
              <td><%= t.team_defense %></td>
              <td><%= t.team_batting_average %></td>
              <td><%= t.team_homerun %></td>
              <td><%= t.team_steal %></td>
              <td><%= t.team_runs %></td>
              <td><%= t.tema_conceded %></td>
              
              
              <td><%=link_to '詳細', "/teams/#{t.id}" %></td>
            </tr>
          </tbody>
          <% end %>
        </table>
      </div>
    <% if user_signed_in? && current_user.admin? %>
      <%=link_to '新規データ入力', new_team_path, class:"back-btn" %>
      <%=link_to 'TOP', root_path, class:"back-btn" %>
    <% end %>
    </main>
  </div>
</div>


  </body>
</html>
